@{
    DateTime currentDate = DateTime.Now;
    if (Session["currentDate"] != null)
    {
        currentDate = Convert.ToDateTime(Session["currentDate"].ToString());
    }
}
<div class="container">
    <ul class="nav navbar-nav">
        <li class="nav-toggle"><a href="javascript:void(0);" title=""><i class="icon-reorder"></i></a></li>
    </ul>

    <a class="navbar-brand" href="@Url.Action("Index", "Home")">
        <strong>LLAH</strong>
    </a>
    <ul class="nav navbar-nav navbar-right">
        <li class="dropdown" id="dayObject">
            <input type="text" id="txtDay" class="form-control date-picker date-universal" value="@currentDate.ToString("dd/MM/yyyy")" />
        </li>
        <li id="notificationErrorsLoad" class="dropdown">
        <li id="progressObjectLoad" class="dropdown hidden-xs hidden-sm">        
        </li>
        @{ Html.RenderPartial("_UserSection", "SharedControls"); }
    </ul>
</div>
